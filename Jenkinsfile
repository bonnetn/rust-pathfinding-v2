pipeline {
    agent { 
      dockerfile {
        dir '/home/jenkins/workspace/pathfinding_v2'
      }
    }
    stages {
        stage('Build') {
            steps {
                sh 'echo test'
            }
        }
    }
}
